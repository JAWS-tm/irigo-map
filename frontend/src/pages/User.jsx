import React from 'react';
import PropTypes from 'prop-types';

const User = (props) => {
  return (
    <div>
      <h2>Users list</h2>
    </div>
  );
};

User.propTypes = {};

export default User;
