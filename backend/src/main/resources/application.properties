# JPA
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.database-platform = org.hibernate.dialect.MySQL5Dialect
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

# JWT
app.jwt.secret=ewsYvxMBxLnMEXvbWFWyI5XssIAtLYCf

# Swagger
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Mailer
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=irigomap@gmail.com
spring.mail.password=gxsdirkqrfolmaui
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000  # TLS , port 587


# SSL / HTTPS
# The format used for the keystore. It could be set to JKS in case it is a JKS file
#server.ssl.key-store-type=PKCS12
## The path to the keystore containing the certificate
#server.ssl.key-store=classpath:pgl_backend.p12
## The password used to generate the certificate
#server.ssl.key-store-password=pgl_backend
#server.ssl.key-password=pgl_backend
## The alias mapped to the certificate
#server.ssl.key-alias=pgl_backend
#
#server.ssl.enabled=true